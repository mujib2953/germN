
export const loggError = (message: string | object) => {
    // tslint:disable-next-line:no-console
    console.error(message);
};

export const logInfo = (message: string) => {
    // tslint:disable-next-line:no-console
    console.info(message);
};

export const logg = (message: string) => {
    // tslint:disable-next-line:no-console
    console.log(message);
};
